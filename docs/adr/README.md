# Architecture decision records

One file per decision: `NNNN-short-title.md`, numbered in order, never edited
after acceptance — supersede it with a new record instead.

Suggested shape: **Context** (what forced the choice), **Decision** (what was
chosen), **Consequences** (what this costs us later).
