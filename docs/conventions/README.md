# Conventions

Shared, stack-independent agreements. Fill these in per fork:

- naming (features, files, state classes, test names)
- branch + commit message format
- error handling: what surfaces to the user vs what is logged
- logging and analytics event naming
- what must have a test before merge
